# Kai2 9000 功能需求规格说明（AI 可直接解析执行）

> **用途**：本文件供 AI 编码助手直接阅读并按条目实现。每条需求均含【目标】【位置】【行为】【验收标准】。
> **项目假设**：Kai2 9000 为开源 AI 助手客户端（参考 Kai 9000 架构），前端 UI + 设置层 + Provider/Model 抽象层 + 工具/MCP 层 + 本地推理层。
> **代码标记约定**：`[NEED_IMPLEMENT]` 表示需 AI 填充实现；`[CONFIG_KEY]` 表示需新增的设置项 key。

---

## 1. 更改提供商名称

- **目标**：支持自定义 Provider（模型服务商）的显示名称。
- **位置**：`Settings > Provider / 模型提供商` 列表；Provider 配置数据结构。
- **行为**：
  - 每个 Provider 配置项增加 `displayName` 字段，缺省时 fallback 到默认名。
  - UI 上名称可编辑（输入框），保存后全局生效（对话列表、模型选择下拉框、消息来源标签）。
- **验收标准**：
  - 修改名称后，所有显示 Provider 的地方同步更新，无需重启。
  - 内置 Provider 默认名作为 placeholder，自定义名持久化到配置。

---

## 2. 增加思考强度按钮

- **目标**：控制推理类模型（如 DeepSeek-R1、o1、Gemini Thinking）的思考深度/推理预算。
- **位置**：聊天输入栏旁 / 模型选择面板；`[CONFIG_KEY]` = `reasoningEffort`。
- **行为**：
  - 提供 3~5 档可选：`低 / 中 / 高 / 最高`（映射为 `minimal / low / medium / high / xhigh`，按 provider 支持的枚举适配）。
  - 仅在当前选中模型支持 reasoning 时显示该按钮。
  - 值写入请求体（如 `reasoning_effort` / `thinking` 参数），透传至对应 API。
- **验收标准**：
  - 切换档位后，下一次请求携带对应参数。
  - 对不支持 thinking 的模型自动隐藏，且不传参。

---

## 3. 设置里增加流式输出开关

- **目标**：允许用户关闭流式（streaming），改为等待完整响应一次性返回。
- **位置**：`Settings > 聊天`；`[CONFIG_KEY]` = `streamingEnabled`，默认 `true`。
- **行为**：
  - 开关关闭时，调用 API 使用非流式模式（`stream=false`），结果整体渲染。
  - 开关实时生效，无需重启会话。
- **验收标准**：
  - 关闭后，消息以整段出现，无逐字流式效果。
  - 开关状态持久化，重启应用后保留。

---

## 4. 提示词字数上限增加

- **目标**：提高系统提示词 / 用户输入的字符上限，支持超长上下文与长提示词。
- **位置**：提示词编辑框（System Prompt）、输入栏；`[CONFIG_KEY]` = `maxPromptLength`，默认提升至 `200000`（20 万字符，可按模型上下文动态调整）。
- **行为**：
  - 移除或显著放宽原有字符/Token 硬限制，改为按所选模型 `max_context` 自适应显示剩余容量。
  - UI 显示 `已用 / 上限` 计数，超限时给出明确提示而非静默截断。
- **验收标准**：
  - 可输入并保存超过原上限的长提示词。
  - 超长内容完整发送至 API，不被前端截断。

---

## 5. 消息操作按钮组（重新生成 / 修改 / 复制 / 删除）

### 5.1 重新生成消息按钮
- **位置**：每条 AI 消息气泡的操作菜单。
- **行为**：沿用相同输入 + 参数重新请求一次，新消息替换或追加（可配置）。
- **验收**：点击后立即重新调用，保留原上下文。

### 5.2 修改消息内容按钮
- **位置**：每条用户消息气泡。
- **行为**：点击后消息变为可编辑输入框，保存后重新触发后续对话（提示用户后续上下文可能失效）。
- **验收**：可就地编辑并保存，修改后高亮标记。

### 5.3 复制消息按钮
- **位置**：每条消息气泡。
- **行为**：复制纯文本；如含代码块/公式，提供「复制为 Markdown」选项。成功给出 Toast。
- **验收**：一键复制，带成功反馈。

### 5.4 删除消息按钮
- **位置**：每条消息气泡。
- **行为**：删除单条消息，并从上下文列表中移除；提供二次确认。
- **验收**：删除后上下文同步更新，不影响持久化存储。

---

## 6. 上下文压缩工具

- **目标**：当对话逼近模型上下文窗口时，自动/手动压缩历史，保留关键信息、降低成本。
- **位置**：`Settings > 上下文`；`[CONFIG_KEY]` = `contextCompression`，可选 `off / auto / manual`。
- **行为**：
  - **Auto**：token 数超过阈值（如窗口 80%）时，自动摘要早期对话为一段 summary，保留最近 N 轮原文。
  - **Manual**：提供「压缩上下文」按钮，手动触发。
  - 压缩后注入系统侧（如 `system: [历史摘要]`），对用户透明。
- **验收标准**：
  - 长对话下自动触发，token 数回落至阈值内。
  - 压缩摘要可见（调试模式）且不丢失用户关键设定。

---

## 7. 增加 Skill 功能

- **目标**：支持可插拔的「技能」系统，扩展 AI 能力（类似 ChatGPT Plugins / Claude Skills）。
- **位置**：`Settings > Skills`；技能目录 `/skills`。
- **行为**：
  - 每个 Skill = 一份清单（name、description、trigger 条件、prompt 片段、可选脚本/工具）。
  - 用户可开关单个 Skill；启用后其描述注入系统提示词，AI 按需调用。
  - 内置基础 Skill 模板（如翻译、总结、代码解释），并支持导入自定义 Skill（Markdown/YAML）。
- **验收标准**：
  - 可启用/禁用 Skill，生效于新对话。
  - AI 在匹配场景自动应用 Skill，并在消息中标注所用 Skill。

---

## 8. MCP 服务器支持编辑

- **目标**：MCP（Model Context Protocol）服务器配置可视化编辑，无需手写 JSON。
- **位置**：`Settings > MCP 服务器`。
- **行为**：
  - 列表展示已配置的 MCP Server（名称、命令/URL、状态、已暴露工具数）。
  - 支持 **新增 / 编辑 / 删除 / 启用禁用**，表单化填写 `command`、`args`、`env`、`url`（SSE/HTTP）。
  - 配置持久化，改动后可「重连」热加载工具列表。
- **验收标准**：
  - 通过 UI 添加的 MCP Server 能被客户端连接并列出工具。
  - 编辑后无需重启即可生效。

---

## 9. MCP 工具人工确认开关

- **目标**：MCP 工具调用前需用户手动批准，防止危险操作（写文件、执行命令、发邮件等）。
- **位置**：`Settings > MCP`；`[CONFIG_KEY]` = `mcpRequireConfirmation`，默认 `true`。
- **行为**：
  - 开启时，每次工具调用弹出确认对话框（展示工具名 + 参数），用户选择「允许本次 / 始终允许此工具 / 拒绝」。
  - 可配置白名单（特定工具免确认）。
  - 关闭时（高风险）给出醒目警告。
- **验收标准**：
  - 默认每次调用前确认；勾选「始终允许」后该工具不再提示。

---

## 10. 增加其他集成工具

> 以下为建议清单（用户未明确，AI 可自行选型与扩展）：

- **网络与检索**：联网搜索、网页抓取（Read URL）、RSS 订阅。
- **文件与数据**：PDF/Word/Excel 解析、CSV 数据分析、图表生成。
- **日历 / 提醒 / 定时任务**：本地提醒、cron 定时触发。
- **通知 / 邮件**：桌面通知、SMTP 发信。
- **代码执行**：沙盒（Pyodide/WASM、Docker、本地 shell）运行代码片段。
- **存储 / 知识库**：本地向量库（RAG）、笔记/书签集成。
- **通讯**：Webhook、Telegram/Discord Bot 推送。
- **实现要求**：统一 `Tool` 抽象接口，支持开关、权限分级、流式输出结果；每个工具含名称、描述、参数 schema、执行函数。

---

## 11. 本地模型增加 Llama.cpp 后端

- **目标**：本地推理除现有后端外，新增 **Llama.cpp**（GGUF）支持。
- **位置**：`Settings > 本地模型 > 后端`；选项 `Ollama / LM Studio / Llama.cpp`。
- **行为**：
  - 集成 `llama-cpp` / `llama-cpp-python` 或 WASM 版本（依平台），加载本地 GGUF 模型文件。
  - 支持选择模型文件路径、设置 threads、context size、GPU layers（如平台支持）。
  - 统一推理接口，使 Llama.cpp 后端与其他后端在聊天流程中无缝切换。
- **验收标准**：
  - 可选中 GGUF 模型并加载，正常进行流式对话。
  - 参数调整（线程数、上下文）生效。

---

## 12. 图像 / 视频生成支持

- **目标**：聊天内直接调用图像、视频生成模型。
- **位置**：输入栏「+」菜单 > 图像生成 / 视频生成；或识别以 `生成图片:`/`生成视频:` 开头的指令自动触发。
- **行为**：
  - **图像**：接入支持的图像 API（OpenAI Images、Gemini Imagen、Replicate、本地 SD/ComfyUI），输入提示词 → 返回图片渲染在消息中，支持多图、尺寸、风格参数。
  - **视频**：接入视频生成（如 Runway/可灵/OpenAI Sora 类 API 或本地），返回视频 URL/文件，支持在消息内播放。
  - 生成结果可保存、复制链接，计入上下文（多模态）。
- **验收标准**：
  - 图像生成后直接展示在对话流，可再次基于图片追问。
  - 视频生成后可内联播放，显示生成状态/进度。

---

## 13. 云端 TTS 服务支持

- **目标**：将 AI 回复文字转为语音，支持多家云端 TTS。
- **位置**：消息操作菜单「朗读」+ `Settings > TTS`。
- **行为**：
  - 支持多家云端 TTS 服务商，可切换：OpenAI TTS、ElevenLabs、Google Cloud TTS、Azure TTS、Deepgram、火山/腾讯云 TTS 等。
  - 设置项：服务商、API Key、语音/音色、语速、音量；`[CONFIG_KEY]` = `ttsProvider`。
  - 消息气泡「朗读」按钮：整段朗读，支持暂停/停止/进度；自动滚动高亮当前句（可选）。
  - 优先使用系统 TTS 作为兜底（无网络/未配置时）。
- **验收标准**：
  - 配置任一服务商后，点击朗读可听到语音且音色切换生效。
  - 朗读中可随时停止，长文本流式朗读不卡顿。

---

## 统一约束（适用于所有需求）

1. **向后兼容**：所有新增设置项均有默认值，老配置可正常加载。
2. **持久化**：设置通过统一配置层（如 `localStorage`/SQLite/配置文件）存取，重启保留。
3. **平台适配**：Android / iOS / Desktop / Web 均需可用；平台特有能力（如本地沙盒）做降级处理。
4. **国际化**：所有新增 UI 文案接入 i18n。
5. **无障碍 & 隐私**：敏感 Key 加密存储；操作有 Toast/日志反馈。

---

## 实现优先级建议（供 AI 排期）

| 优先级 | 条目 |
|---|---|
| P0（核心体验） | 2、3、5、4 |
| P1（扩展能力） | 1、6、7、8、9 |
| P2（高级/生态） | 10、11、12、13 |

---

**使用说明**：AI 编码时，请逐条（按编号）实现并在代码/PR 中标注对应编号（如 `// [REQ-5.1]`），实现完一条即在下方 `[进度]` 打勾，便于追踪。

## 进度
- [x] 1. 更改提供商名称 ✅ 已有：`renameServiceInstance` + displayName 持久化，设置页可编辑，全局生效（v3.2.2 已实现）
- [x] 2. 思考强度按钮 ✅ 已实现：设置页「思考强度」选择器（默认/低/中低/中/高/最高），写入 `reasoning_effort` 请求体（v3.2.3+）
- [x] 3. 流式输出开关 ✅ 已实现：设置页「流式输出」开关，关闭时 `stream=false` 整体渲染（v3.2.3+）
- [x] 4. 提示词字数上限 ✅ 已满足：输入框无字符硬截断，文件上限 200KB（20 万字符级），超长内容完整发送
- [x] 5.1 重新生成 ✅ 已有：BotMessage `onRegenerate` 按钮，保留上下文重新请求
- [ ] 5.2 修改消息 🔲 规划中：需要 ChatViewModel 编辑 action
- [x] 5.3 复制消息 ✅ 已有：BotMessage 复制按钮（含代码块复制）
- [x] 5.4 删除消息 ✅ 已实现：每条消息操作栏「删除」按钮（AI 消息 × 图标 / 用户消息文字），删除单条及其配套工具行，自动保存（v3.2.3-2）
- [x] 6. 上下文压缩 ✅ 已有：`compactHistoryIfNeeded`（70% 阈值自动摘要 + 保留最近 4 轮）
- [x] 7. Skill 功能 ✅ 已有：完整 skills 包（SkillManager/Registry/Marketplaces/前端 SkillsSection）
- [x] 8. MCP 编辑 ✅ 已有：完整 mcp 包（McpServerManager/Client + 设置页 McpSection 增删改启停）
- [x] 9. MCP 确认开关 ✅ 已实现：设置→工具→「MCP 工具需人工确认」开关（默认开），开启时系统提示词注入确认策略，AI 调用 MCP 工具前必须先展示参数并获用户同意（v3.2.3-3）
- [x] 10. 集成工具 ✅ 已有：web_search/shell/日历/提醒/邮件/通知/文件/定时任务等大量工具，统一 Tool 抽象
- [ ] 11. Llama.cpp 后端 🔲 规划中：现有 LiteRT/Ollama，需新增 GGUF 加载
- [ ] 12. 图像/视频生成 🔲 规划中：需接入图像/视频 API
- [ ] 13. 云端 TTS 🔲 规划中：现有系统 TTS，需接入云端服务商

> ✅ = 已实现（代码已在） | 🔲 = 规划中（未实现）
